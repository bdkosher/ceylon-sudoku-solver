interface Solvable {
	
	doc("Returns true if solved.")
	shared formal Boolean isSolved();
	
	//doc("Clears the value at the given row and column number")
	//shared formal void clear(Integer rowNumber, Integer colNumber);
	//
	//doc("Sets the value at the given row and column number")
	//shared formal void fill(Integer rowNumber, Integer colNumber, Integer? val);
	//
	//doc("A completed grid is one that is entirely non-empty")
	//shared formal Boolean isCompleted();
}